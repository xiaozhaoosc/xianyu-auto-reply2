# 问题诊断报告：18:03 订单发货失败 & 奶芙泡泡无AI回复

- 诊断日期：2026-08-30
- 账号：2221611389164（主账号）

---

## 一、订单 3316376607085017787 发货失败（fail_reason="订单状态节点解析失败"）

### 完整链路（scheduler.log / websocket.log 实锤）

| 时间 | 事件 |
|---|---|
| 18:03:18 | 买家付款推送到达 |
| 18:03:46 | 定时补发货处理，`check_can_ship` 通过（已付款可发货），**卡券匹配正常** |
| 18:03:47 | 规格补全报错 `'OrderStatusChecker' object has no attribute '_parse_order_detail_response'`（8/16 已诊断的老 Bug，`redelivery_task.py` 调错兄弟类方法，仅警告不阻断） |
| 18:03:47 | 订单缺 chat_id → 调闲鱼 `/r/SingleChatConversation/create` → **返回 code:400 无 cid** → 写入占位 chat_id |
| 18:03:53 起 | 每 6~7 秒重试，均因占位 chat_id 直接跳过发货（死循环） |
| 18:07:20 | 买家发起退款申请 |
| 18:09 | 订单转 refunding，check_can_ship 再查详情返回退款态结构（无 orderStatusVO 节点）→ fail_reason 覆写为"订单状态节点解析失败" |

### 结论
- **与 8/16 案例（DIAGNOSIS_20260816.md）完全同一根因**：创建会话 400 + 占位 chat_id 死循环，卡券不是原因。
- "订单状态节点解析失败"是**买家退款后的表象**，不是首发原因。
- 该买家其实已有会话（推送 cid=66011013726），创建会话接口 400 本可改用查询既有会话回填。

### 待修复（新分支，待用户拍板）
1. `redelivery_task.py` `_parse_order_detail_response` 调错类（AttributeError）。
2. 创建会话 400 时走会话列表查询回填 chat_id，而非写占位 ID。
3. 占位 chat_id 不应永久阻断，应允许带退避的重试。

---

## 二、奶芙泡泡 16:17 消息无 AI 回复

### 现象
- 16:17:58–16:18:33 四条消息正常到达（backend-web 推送日志完整）。
- AI 配置正常：enabled、模型可用（同日 14:07 AI 回复成功过）、买家无订单（非已下单拦截场景）。

### 根因：**自动回复 IM 连接僵尸化**
- 14:08:03 9164 自动回复 WebSocket 断开（ConnectionClosedError），14:08:09 自动重连成功。
- **重连后心跳正常、状态 connected，但闲鱼不再推业务消息**：14:08:09 后 `解密消息` 0 条，持续 5 小时 45 分；期间最后一次正常自动回复是 14:07:33（14:08:03 发送成功）。
- 心跳探测无法发现这种僵尸状态 → 系统自认为一切正常。

### 处置
- 19:54 通过内部 API `POST /internal/accounts/2221611389164/restart` 重启账号任务，连接重建、监听恢复，自动回复已恢复。

### 建议（待用户拍板）
- 增加"消息断流看门狗"：backend-web 推送通道有消息而 ws 自动回复连接连续 N 分钟 0 条解密消息 → 自动重启该账号任务 + 飞书告警。
