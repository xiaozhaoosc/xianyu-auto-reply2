# 闲鱼自动回复系统 - 采集功能增强开发记录

> 开发日期：2026-06-11
> 开发人员：SmallSteps (AI Assistant)
> 项目路径：`~/Documents/hermesgit/xianyu-auto-reply/`

---

## 一、工作概述

本次开发针对 xianyu-auto-reply 系统的**商品采集功能**进行了全面增强，解决了采集结果无法使用的核心问题，并新增了多种采集方式。

### 完成的任务

| 序号 | 任务 | 状态 |
|------|------|------|
| 1 | 采集结果数据流分析 | ✅ 完成 |
| 2 | 采集→素材桥接功能 | ✅ 完成 |
| 3 | 按 ID 采集功能 | ✅ 完成 |
| 4 | 按卖家采集功能（带筛选） | ✅ 完成 |
| 5 | 多账号商品同步功能 | ✅ 完成 |
| 6 | 网盘链接自动发货研究 | ✅ 完成 |

---

## 二、技术架构分析

### 2.1 系统架构

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   Frontend   │────▶│ Backend-Web  │────▶│  WebSocket   │
│   (:9000)    │     │   (:8089)    │     │   (:8090)    │
└──────────────┘     └──────────────┘     └──────────────┘
                            │                     │
                            ▼                     ▼
                     ┌──────────────┐     ┌──────────────┐
                     │   MySQL      │     │   Goofish    │
                     │ (xianyu_data)│     │   Platform   │
                     └──────────────┘     └──────────────┘
```

### 2.2 核心数据库表

| 表名 | 用途 | 关键字段 |
|------|------|----------|
| `xy_goofish_crawl_items` | 采集结果存储 | item_id, title, price, want_count, view_count |
| `xy_product_materials` | 素材库 | title, description, price, images |
| `xy_catalog_items` | 商品目录 | item_id, title, price, account_id |
| `xy_accounts` | 账号管理 | account_id, cookie, status |

### 2.3 原有问题

**采集结果是"死胡同"**：
- 采集数据存入 `xy_goofish_crawl_items` 后只能查看，无法使用
- 没有导入素材库的功能
- 没有按 ID/卖家采集的功能

---

## 三、新增功能详细说明

### 3.1 采集→素材桥接

**功能**：将采集结果批量导入素材库，支持筛选条件。

**API 端点**：`POST /api/v1/goofish/crawler/import-to-materials`

**请求参数**：
```json
{
  "job_id": 123,
  "item_ids": ["1014128530852"],
  "min_want_count": 10,
  "min_view_count": 100
}
```

**字段映射**：
```
crawl_items.title       → materials.title
crawl_items.description → materials.description
crawl_items.price       → materials.price
crawl_items.main_image  → materials.images[0]
crawl_items.area        → materials.address
want_count + view_count → materials.remark
```

**代码改动**：
- 后端：`backend-web/app/api/routes/goofish_crawler.py`
- 前端 API：`frontend/src/api/goofishCrawler.ts`
- 前端 UI：`frontend/src/pages/crawler/GoofishScheduledCrawler.tsx`

---

### 3.2 按 ID 采集

**功能**：通过 item_id 直接采集单个商品详情。

**API 端点**：`POST /api/v1/goofish/crawler/fetch-by-id`

**请求参数**：
```json
{
  "item_ids": ["1014128530852", "1014128530853"],
  "cookie_id": "account_001"
}
```

**实现原理**：
1. 导航到 `https://www.goofish.com/item?id={item_id}`
2. 拦截详情 API 响应（`mtop.taobao.idle.detail`）
3. 从 DOM 提取补充信息
4. 处理滑块验证码

**代码改动**：
- 服务层：`backend-web/app/services/compass/goofish_compass.py` 新增 `fetch_by_item_id()` 方法

---

### 3.3 按卖家采集（带筛选）

**功能**：访问卖家主页，提取所有商品，支持阈值筛选。

**API 端点**：`POST /api/v1/goofish/crawler/fetch-by-seller`

**请求参数**：
```json
{
  "user_ids": ["2216005943308"],
  "cookie_id": "account_001",
  "min_want_count": 100,
  "min_view_count": 300,
  "max_pages": 5
}
```

**实现原理**：
1. 导航到 `https://www.goofish.com/personal?userId={user_id}`
2. 从 DOM 提取商品卡片
3. 自动滚动翻页 + 点击"加载更多"
4. 按想要人数/浏览量筛选
5. 自动去重

**DOM 提取逻辑**：
```javascript
// 选择器兼容多种页面结构
'[class*="item-card"], [class*="feed-item"], [class*="product-card"], a[href*="/item?id="]'

// 提取字段
item_id, title, price, main_image, want_count, view_count
```

**代码改动**：
- 服务层：`goofish_compass.py` 新增 `fetch_by_seller()` + `_extract_items_from_page()` + `_scroll_next_page()`

---

### 3.4 多账号商品同步

**功能**：从源账号同步商品到目标账号。

**API 端点**：`POST /api/v1/account-sync/sync`

**请求参数**：
```json
{
  "source_account_id": "account_001",
  "target_account_ids": ["account_002", "account_003"],
  "keyword_filter": "iPhone",
  "min_price": 1000,
  "max_price": 5000,
  "sync_mode": "material"
}
```

**同步流程**：
1. 从源账号获取商品列表（ItemService）
2. 按关键词/价格筛选
3. 转为素材（ProductMaterial）
4. 调用批量发布（PublishExecutorService.batch_publish）

**新增文件**：
- `backend-web/app/services/account_sync_service.py` — 同步服务
- `backend-web/app/api/routes/account_sync.py` — API 路由

---

### 3.5 网盘链接自动发货

**结论**：系统已原生支持，无需新增代码。

**使用方法**：
1. 卡券管理 → 创建卡券 → 类型选择 `text`
2. `text_content` 填写下载链接：
   ```
   百度网盘链接: https://pan.baidu.com/s/xxx
   提取码: abc1
   ```
3. 绑定卡券到商品
4. 买家付款后自动发送

**支持的变量替换**：
- `{order_id}` — 订单号
- `{buyer_name}` — 买家昵称
- `{item_title}` — 商品标题
- `{seller_name}` — 卖家昵称

---

## 四、GitHub 参考项目调研

| 项目 | Stars | 技术栈 | 参考价值 |
|------|-------|--------|----------|
| **Usagi-org/ai-goofish-monitor** | 12.5k | Playwright + FastAPI + SQLite | 多任务并发、筛选条件、反检测、登录态管理 |
| LENKIN233/xianyu-monitor-skill | 6 | Playwright + AI | AI 驱动监控，自然语言创建任务 |
| superboyyy/xianyu_spider | 753 | Playwright + FastAPI | 关键词搜索 + 数据去重 |
| pbeenigg/LittleCrawler | 720 | Playwright + Python | 多平台框架（xhs/zhihu/xhy） |
| hlwtl/xy | 35 | 闭源 | 数据过滤模块概念参考 |

---

## 五、关键代码位置

| 功能 | 文件路径 | 方法/端点 |
|------|----------|-----------|
| 采集引擎 | `backend-web/app/services/compass/goofish_compass.py` | `GoofishCompassService` |
| 按 ID 采集 | `goofish_compass.py` | `fetch_by_item_id()` |
| 按卖家采集 | `goofish_compass.py` | `fetch_by_seller()` |
| DOM 提取 | `goofish_compass.py` | `_extract_items_from_page()` |
| 翻页逻辑 | `goofish_compass.py` | `_scroll_next_page()` |
| 导入素材 API | `backend-web/app/api/routes/goofish_crawler.py` | `POST /import-to-materials` |
| 按 ID 采集 API | `goofish_crawler.py` | `POST /fetch-by-id` |
| 按卖家采集 API | `goofish_crawler.py` | `POST /fetch-by-seller` |
| 账号同步服务 | `backend-web/app/services/account_sync_service.py` | `sync_items_between_accounts()` |
| 账号同步 API | `backend-web/app/api/routes/account_sync.py` | `POST /sync` |
| 批量发布 | `backend-web/app/services/publish_execution_service.py` | `batch_publish()` |

---

## 六、踩坑记录

### 6.1 滑块验证码
- 所有采集操作都需要处理验证码
- 确保账号开启了"显示浏览器"选项
- 验证码处理代码：`slider_handler.handle_verification()`

### 6.2 Cookie 有效性
- 采集前检查 Cookie 是否过期
- 过期 Cookie 会导致抓取失败
- 定期使用 Cookie 刷新功能

### 6.3 DOM 选择器兼容性
- 闲鱼页面结构可能变化
- `_extract_items_from_page()` 中使用多种选择器兼容
- 需要定期验证选择器有效性

### 6.4 反爬限制
- 频繁采集可能触发风控
- 建议设置合理的间隔（`interval_seconds >= 60`）
- 设置最大页数限制（`max_pages <= 20`）

### 6.5 素材去重
- 导入素材库时按标题去重
- 避免重复创建相同素材
- 去重逻辑在 `import_to_materials` 中实现

### 6.6 Pyright 静态分析警告
- 项目中的 `common.*` 导入在静态分析时报错
- 运行时正常（Python path 配置问题）
- 可忽略这些警告

---

## 七、部署说明

### 7.1 环境要求
- Python 3.11+
- Node.js 18+
- MySQL 8.0
- Redis 7
- Playwright + Chromium

### 7.2 部署步骤
```bash
# 1. 进入项目目录
cd ~/Documents/hermesgit/xianyu-auto-reply

# 2. 部署所有服务
bash deploy.sh -ss-all

# 3. 验证服务状态
curl http://localhost:8089/health
```

### 7.3 数据库迁移
本次开发无需数据库迁移，所有功能使用现有表结构。

---

## 八、后续优化建议

1. **采集结果导出**：支持导出为 CSV/Excel 格式
2. **采集任务调度**：支持定时采集 + 自动导入素材库
3. **采集结果分析**：添加价格趋势、热度分析等功能
4. **多平台支持**：扩展到其他二手平台（转转等）
5. **AI 智能筛选**：使用 AI 自动识别高价值商品

---

## 九、相关文档

- [闲鱼自动回复系统部署文档](./xianyu-auto-reply-deploy.md)
- [数据库 Schema](./xianyu_schema.sql)
- [技术分析报告](./TECHNICAL_ANALYSIS.md)（项目根目录）

---

## 十、Skill 参考

本次开发的所有功能已保存为 Hermes Skill：
- **Skill 名称**：`xianyu-auto-reply-enhancements`
- **类别**：devops
- **路径**：`~/.hermes/skills/devops/xianyu-auto-reply-enhancements/SKILL.md`

使用方法：
```
skill_view(name='xianyu-auto-reply-enhancements')
```
